{
  "name": "name-generator-action",
  "version": "1.2.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "build:action": "npm run build && ncc build lib/action.js -o dist --source-map",
    "package": "npm run build:action",
    "test": "vitest --run"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "@vercel/ncc": "^0.38.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  }
}
